pipeline {
  agent any
  options{
    buildDiscared(logRotator(numToKeepStr: '2'))
  }
  stages {
    stage('Say Hello'){
      steps{
        echo 'Hello World!'
        sh 'java -version'
      }
    }
  }
}
